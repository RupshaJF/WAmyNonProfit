# সদস্য লগইন v2 — আপগ্রেড গাইড

লগইন সেকশন (`#/login`) নতুন করে বানানো হয়েছে — পিওর জাভাস্ক্রিপ্ট + পিওর CSS, কোনো লাইব্রেরি নেই,
কোনো গ্লো/লাইটিং/গ্র্যাডিয়েন্ট/ব্লার ইফেক্ট নেই। Firestore-এর কাঠামো, admin panel, EmailJS template
ও `firestore.rules` **একটুও বদলাতে হবে না** — আগের ব্যবস্থার সাথেই সরাসরি কাজ করে।

> ⚠️ এই গাইডের **§ ৫ (নিরাপত্তা)** অবশ্যই পড়ুন — ব্যাকএন্ডে একটা গুরুতর দুর্বলতা আছে,
> যা শুধু ফ্রন্টএন্ড বদলে ঠিক করা যায় না।

---

## ১. ফাইল তালিকা

| ফাইল | অবস্থা | কাজ |
|---|---|---|
| `js/login-loader.js` | **নতুন** (~২.৫ KB gzip) | `#/login` খুললে তবেই বাকি ফাইল লোড করে; হোম পেজ হালকা থাকে |
| `js/login-core.js` | **নতুন** | আইকন (নিজস্ব SVG), তারিখ/সময় (বাংলা), টোস্ট, ক্লিপবোর্ড, নিরাপদ র‍্যান্ডম |
| `js/login-auth.js` | **নতুন** | আইডি/ইমেইল/মোবাইল শনাক্তকরণ, লগইন, ভুল-চেষ্টার সীমা, সেশন, পাসওয়ার্ড রিসেট |
| `js/login-qr.js` | **নতুন** | পিওর জাভাস্ক্রিপ্ট QR জেনারেটর (অফলাইনেও কাজ করে) |
| `js/login.js` | **প্রতিস্থাপিত** | কনফিগ (মাথায়) + পেজ/ফর্ম/ড্যাশবোর্ড/ডায়ালগ |
| `js/dashboard-notices.js` | **প্রতিস্থাপিত** | নোটিশ ট্যাব |
| `js/dashboard-events.js` | **প্রতিস্থাপিত** | ইভেন্ট ট্যাব |
| `css/login.css` | **প্রতিস্থাপিত** | সম্পূর্ণ নতুন ডিজাইন |
| `index.html` | ২টি ছোট পরিবর্তন | নিচে দেখুন |
| `sw.js` | ১ লাইন | `CACHE_VERSION` বাড়ানো |

## ২. ইনস্টল

1. জিপটা প্রজেক্টের রুট ফোল্ডারে এক্সট্র্যাক্ট করুন (`Replace` চাপুন — পাথ আগের মতোই)।
2. **যদি `index.html` / `sw.js` আপনি আলাদাভাবে বদলে থাকেন**, ফাইল প্রতিস্থাপন না করে শুধু এই পরিবর্তনগুলো করুন:
   - `index.html`: `<link rel="stylesheet" href="css/login.css">` লাইনটা **মুছুন**।
   - `index.html`: `login.js`, `dashboard-notices.js`, `dashboard-events.js` — এই তিন `<script>` লাইনের জায়গায়
     শুধু `<script src="js/login-loader.js"></script>` বসান (`router.js`-এর আগেই থাকতে হবে)।
   - `sw.js`: `CACHE_VERSION` বাড়ান (যেমন `'rjf-v1.04.0'`)।
3. Deploy করুন। **প্রতিবার লগইন ফাইল বদলে deploy করার আগে `sw.js`-এর `CACHE_VERSION` বাড়াবেন** —
   না হলে ফিরতি ভিজিটররা পুরনো ক্যাশ থেকে পুরনো কোড দেখতে পারেন।
4. `js/login.js`-এর মাথার কনফিগ (Firebase / EmailJS key) আগের মতোই — নতুন ফাইলে আপনার বর্তমান মানগুলো বসানো আছে।
   আপনি কোনো key বদলে থাকলে (বিশেষ করে EmailJS) মিলিয়ে নিন।

## ৩. নতুন কী কী

**লগইন ফর্ম**
- একটাই স্মার্ট ঘরে **সদস্য আইডি / ইমেইল / মোবাইল** — টাইপ করতে করতেই ধরে নেয় (আইকন বদলায়)।
- বাংলা অঙ্ক চলে (`RJF-২০২৬-১২৩৪`), স্পেস/ছোট হাতের/ভুল ড্যাশ ঠিক করে নেয়;
  শুধু `1234` লিখলেও চলে (গত বছরগুলোতেও খোঁজে; পাসওয়ার্ড মিললে সঠিক সদস্যকে বেছে নেয়)।
- পাসওয়ার্ড দেখান/লুকান, **Caps Lock সতর্কতা**, পাসওয়ার্ড ম্যানেজার-বান্ধব, পেস্টে বাড়তি স্পেস সমস্যা নেই।
- ভুল-ধরন ও খালি ঘরের বার্তা ঘরের নিচেই, ফোকাস প্রথম ভুল ঘরে; অফলাইন ব্যানার; নেটওয়ার্ক হ্যাং হলে টাইমআউট বার্তা।
- **ভুল-চেষ্টার সীমা:** ৫ বার ভুলে ৩০ সে. লক, তারপর ৬০ সে., ২ মি., ৫ মি., ১৫ মি.; রিলোডেও লক থাকে।
- ভুল হলে বার্তা সাধারণ (কোনটা ভুল তা বলে না)।

**সেশন**
- মেয়াদ: সাধারণ ৮ ঘণ্টা; **"এই ডিভাইসে লগইন থাকুন"** চাপলে ৩০ দিন।
- **২০ মিনিট নিষ্ক্রিয় থাকলে** ৬০ সেকেন্ডের সতর্কতাসহ অটো-লগআউট (লগইন-থাকা সেশনে নয়)।
- এক ট্যাবে লগআউট করলে অন্য ট্যাবও লগআউট।
- ব্যাকগ্রাউন্ডে যাচাই: সদস্যপদ `blocked` হলে বা **পাসওয়ার্ড বদলালে** অন্য ডিভাইসের সেশন নিজে থেকে শেষ।
- `blocked`/`rejected` সদস্য লগইন করতে পারবেন না; `pending` ঢুকতে পারেন, উপরে "অনুমোদনের অপেক্ষায়" নোটিস দেখায়।

**পাসওয়ার্ড রিসেট**
- নতুন পাসওয়ার্ড এখন **crypto-নিরাপদ** (`Math.random` নয়), দেখতে-একই-রকম অক্ষর (`0 O 1 l I`) বাদ।
- আইডি/ইমেইল/মোবাইল যেটাই দিন চলবে; ইমেইল **মাস্ক করে** দেখায় (`Ka•••@example.com`)।
- ইমেইল না গেলে স্পষ্ট বলে ও **একই পাসওয়ার্ড আবার পাঠানো** যায় (দ্বিতীয়বার বদলায় না — সদস্য আটকে যান না)।
- ৬০ সেকেন্ড কুলডাউন, ঘণ্টায় সর্বোচ্চ ৫ বার (এই ডিভাইসে)।
- EmailJS এখন সরাসরি REST API-তে (SDK ও পোলিং লুপ লাগে না)।

**ড্যাশবোর্ড**
- **ডিজিটাল সদস্য কার্ড**: ছবি, নাম, আইডি, স্ট্যাটাস, ও সরাসরি স্ক্যানযোগ্য QR।
  QR এখন নিজস্ব জেনারেটরে (CDN নয়) — অফলাইনেও দেখায়।
- QR ডায়ালগ: বড় QR, **লিংক কপি, শেয়ার (ফোনে), কার্ডের ছবি সেভ (PNG), ভেরিফাই পেজ খুলুন**।
- আইডি/মোবাইল/ইমেইল এক ট্যাপে কপি; ছবি না থাকলে/না খুললে নামের আদ্যক্ষর।
- **নোটিশ**: পিন করা উপরে, "নতুন" চিহ্ন (গত ১৪ দিনের অদেখাগুলোতে), ট্যাবে ডট, লম্বা লেখায় "আরও পড়ুন",
  লেখার ভেতরের লিংক ক্লিকযোগ্য (নিরাপদভাবে), "২ ঘণ্টা আগে" ধাঁচের সময়।
- **ইভেন্ট**: "আজ / আগামীকাল / ১০ দিন পর", ট্যাবে সংখ্যা, **"ক্যালেন্ডারে যোগ করুন" (.ics)** —
  সময় বাংলাদেশ টাইমজোনে ধরা।
- লোডিং স্কেলেটন, ব্যর্থ হলে "আবার চেষ্টা করুন", সবই রিয়েল-টাইম।

**গতি ও অ্যাক্সেসিবিলিটি**
- লগইনের কোড হোম পেজে লোড হয় না (আগে সবার ব্রাউজারে নামত); Font Awesome/EmailJS SDK/QR লাইব্রেরি নির্ভরতা নেই।
  লগইন খোলার সম্ভাবনা আছে এমন ব্যবহারকারীর জন্য (আগে লগইন করা, বা লগইন লিংকে হাত/মাউস গেলে) আগে থেকে প্রিফেচ;
  Data Saver/2G-তে প্রিফেচ হয় না।
- ARIA ট্যাব (তীর কী, Home/End), নেটিভ `<dialog>` (ফোকাস ট্র্যাপ, Esc), স্ক্রিন-রিডার বার্তা,
  স্পষ্ট ফোকাস রিং, WCAG AA কনট্রাস্ট, `prefers-reduced-motion`, ফোন-ফার্স্ট (৩২০px থেকে), টাচ-ডিভাইসে আটকে থাকা hover নেই।

**গ্লো ইফেক্ট নেই:** কোনো গ্লো/নিয়ন/ব্লার/গ্র্যাডিয়েন্ট/টেক্সট-শ্যাডো নেই। শুধু কার্ড, ডায়ালগ ও টোস্টের নিচে
সামান্য নিরপেক্ষ ছায়া আছে (উঁচু দেখানোর জন্য)। সেটাও না চাইলে `css/login.css`-এর শেষে
`.lp *:not(input) { box-shadow: none !important; }` যোগ করুন।

## ৪. কনফিগ অপশন (`js/login.js`-এর মাথায়)

| অপশন | ডিফল্ট | কাজ |
|---|---|---|
| `deniedStatuses` | `["blocked","rejected"]` | এই status-এ লগইন/রিসেট বন্ধ |
| `pendingStatuses` | `["pending"]` | এই status-এ "অপেক্ষায়" নোটিস |
| `idMinYear` | `2025` | শুধু `1234` লিখলে কত সাল পর্যন্ত পিছিয়ে খুঁজবে |
| `rememberDays` | `30` | "লগইন থাকুন" কতদিন |
| `sessionHours` | `8` | সাধারণ সেশনের সর্বোচ্চ মেয়াদ |
| `idleMinutes` / `idleWarnSeconds` | `20` / `60` | নিষ্ক্রিয়তার সীমা (`0` = বন্ধ) / সতর্কতার সময় |
| `maxAttempts` / `lockSeconds` | `5` / `[30,60,120,300,900]` | ভুল-চেষ্টার সীমা ও লকের ধাপ |
| `passwordLength` | `12` | নতুন পাসওয়ার্ডের দৈর্ঘ্য (Firestore rule: ৮–৬৪) |
| `resetCooldownSeconds` / `resetMaxPerHour` | `60` / `5` | রিসেট সীমা |
| `eventTzOffsetMin` | `360` | ইভেন্টের সময়ের টাইমজোন (বাংলাদেশ = UTC+6) |

## ৫. ⚠️ নিরাপত্তা — ব্যাকএন্ডের দুর্বলতা (গুরুত্বপূর্ণ)

নতুন লগইন ফ্রন্টএন্ডে যতটা সম্ভব শক্ত করা হয়েছে, কিন্তু **যাচাই এখনো ব্রাউজারে হয়**:
পাসওয়ার্ড Firestore-এর `members` ডকুমেন্টের `stored_password` ফিল্ডে **সাধারণ টেক্সটে** রাখা,
আর `firestore.rules` অনুযায়ী:

1. **`members` সবাই পড়তে পারে** (`allow read: if true`) — অর্থাৎ যে কেউ (লগইন ছাড়াই) সব সদস্যের
   পাসওয়ার্ড, ইমেইল, মোবাইল, ঠিকানা, রক্তের গ্রুপ পড়তে পারে। Firebase config ওয়েবপেজেই খোলা, তাই এটা কয়েক লাইনের কাজ।
2. **যে কেউ যেকোনো সদস্যের `stored_password` বদলে দিতে পারে** (`allow update` — শুধু ৮–৬৪ অক্ষরের শর্ত)।
   তাই লগইন ছাড়াই যেকোনো সদস্যের অ্যাকাউন্ট দখল বা তাঁকে লক আউট করা সম্ভব।
3. পাসওয়ার্ড ইমেইলে **সাধারণ টেক্সটে** যায় (সদস্য নিজে বদলাতে পারেন না)।
4. সেশন শুধু ব্রাউজারের storage-এ — DevTools থেকে বানিয়ে ফেলা যায়।
   (এখন ড্যাশবোর্ডে যা দেখা যায় সবই এমনিতেই পাবলিক ডেটা, তাই লগইন আসলে কিছুই আটকায় না।)

ফ্রন্টএন্ডের ভুল-চেষ্টার সীমা, ইমেইল কুলডাউন ইত্যাদি **সাধারণ ব্যবহারকারীর** জন্য সুরক্ষা; কেউ সরাসরি
Firestore-এ কথা বললে এগুলো আটকায় না।

**সঠিক সমাধান (পরের ধাপ):** Firebase Authentication (Email/Password) ব্যবহার —
- সদস্য নিজের পাসওয়ার্ড নিজে সেট করবেন (Firebase-ই রিসেট লিংক পাঠায়; সাধারণ টেক্সট পাসওয়ার্ড আর কোথাও থাকবে না),
- `members` কালেকশন শুধু মালিক (ও admin) পড়তে পারবেন; `verify.html`-এর জন্য আলাদা `public_members` (শুধু নাম/আইডি/স্ট্যাটাস/ছবি),
- অ্যাডমিন প্যানেল ও `firestore.rules` সেই অনুযায়ী বদলাতে হবে।
এতে লগইন পেজের রূপ প্রায় একই থাকে; বদলায় ভেতরের অংশ (`login-auth.js`-এর `authenticate`/`reset` — এগুলো আলাদা করে রাখা আছে,
তাই বদলানো সহজ)। এর জন্য Firebase Console-এ কিছু ধাপ ও কিছু ডেটা মাইগ্রেশন লাগবে — চাইলে ধাপে ধাপে করা যায়।

**এখনই, কোড ছাড়াই করার মতো ছোট সুরক্ষা:**
- EmailJS অ্যাকাউন্টের Security সেটিংসে ডোমেইন সীমাবদ্ধ করুন — শুধু আপনার সাইটের ডোমেইন (নইলে Public Key খোলা থাকায় যে কেউ আপনার কোটায় ইমেইল পাঠাতে পারে)।
- অ্যাডমিন প্যানেল থেকে হাতে পাসওয়ার্ড দিলে সহজ পাসওয়ার্ড (যেমন `12345678`) দেবেন না।
- সদস্যদের সংবেদনশীল তথ্য (ঠিকানা, রক্তের গ্রুপ) `members`-এ রাখা তথ্যের মধ্যে সীমিত রাখুন — যতদিন ডেটা পাবলিক-রিডেবল।

## ৬. পরীক্ষা

স্বয়ংক্রিয়ভাবে ২৪১টি পরীক্ষা চালানো হয়েছে (হেডলেস Chromium, ফেক Firestore/EmailJS দিয়ে) — সবগুলো পাস:
ফর্ম যাচাই ও আইডি-শনাক্তকরণ, ১২ ধরনের লগইন (আইডি/ইমেইল/মোবাইল/বাংলা অঙ্ক/স্পেস/শুধু ৪ ডিজিট…),
ভুল/ব্লক/পেন্ডিং/নেটওয়ার্ক-ফেল/টাইমআউট, লকআউট (রিলোডে টিকে থাকা সহ), সেশন (রিলোড, নতুন ট্যাব, ক্রস-ট্যাব লগআউট,
মেয়াদ, পাসওয়ার্ড-বদল ও ব্লক হলে বাতিল), রিসেট (সব ত্রুটির পথ, ইমেইল-ফেল→রিসেন্ড, কুলডাউন),
নোটিশ/ইভেন্ট (রিয়েল-টাইম, ফিল্টার, `.ics`), **QR ও ডাউনলোড করা কার্ডের ছবি OpenCV দিয়ে ডিকোড করে যাচাই**
(ভার্সন ১–১০ জুড়ে ৩৪টি ডেটায়), XSS চেষ্টা (নাম/নোটিশ/ইভেন্টে ক্ষতিকর কোড — কিছুই চলে না),
নিষ্ক্রিয়তা/অফলাইন/ন্যাভিগেশন, অ্যাক্সেসিবিলিটি ট্রি, ৩২০px–১৪৪০px-এ ওভারফ্লো, লেজি-লোডার (ব্যর্থতা ও retry সহ)।

**যা এখানে পরীক্ষা করা যায়নি (আপনি একবার দেখে নিন):**
- আসল Firebase ও আসল EmailJS-এর সাথে (আমি নকল দিয়ে চালিয়েছি; কলগুলো আপনার আগের কোডের মতোই)।
  EmailJS-এর REST কল SDK-র ভেতরের কলের সমান — তবু প্রথমে নিজের ইমেইলে একবার "পাসওয়ার্ড পাঠান" পরীক্ষা করুন।
- আসল ফোন/ফন্টে চেহারা (আমার পরীক্ষায় Hind Siliguri লোড হয়নি — ফলব্যাক ফন্টে দেখেছি)।

**আপনার ১০ মিনিটের চেকলিস্ট:**
1. `#/login` খুলে ফোনে ও ডেস্কটপে দেখুন। 2. ভুল পাসওয়ার্ড ৫ বার → লক দেখুন।
3. একটা আসল সদস্যের আইডি/ইমেইল/মোবাইল দিয়ে লগইন। 4. "পাসওয়ার্ড পাননি বা ভুলে গেছেন?" → নিজের ইমেইলে পাঠান।
5. ড্যাশবোর্ডে QR স্ক্যান করে ভেরিফাই পেজ খুলে দেখুন। 6. একটা নোটিশ ও ইভেন্ট admin থেকে যোগ করে রিয়েল-টাইমে আসছে কিনা দেখুন।
7. "লগইন থাকুন" দিয়ে ব্রাউজার বন্ধ করে আবার খুলুন। 8. এক সদস্যকে `blocked` করে দেখুন লগইন হচ্ছে না।

## ৭. পরের সম্ভাব্য ধাপ
- **Firebase Authentication-এ মাইগ্রেশন** (§ ৫) — সবচেয়ে গুরুত্বপূর্ণ।
- অ্যাডমিন লগইন পেজ (`.tz/.js/admin/login.html`) একই ডিজাইনে (সেটা আগে থেকেই Firebase Auth ব্যবহার করে)।
- ডার্ক মোড (রঙ টোকেন আলাদা করা আছে, যোগ করা সহজ)।
