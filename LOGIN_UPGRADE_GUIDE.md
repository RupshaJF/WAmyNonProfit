# নিরাপত্তা আপগ্রেড গাইড — সম্পূর্ণ (Firestore Rules + Firebase Authentication)

এই গাইড আগের `LOGIN_UPGRADE_GUIDE.md`-এর জায়গায়। লগইন সেকশন এখন শুধু নতুন ডিজাইনেই নয় —
**পুরো অথেন্টিকেশন ব্যবস্থাটাই** বদলে দেওয়া হয়েছে, কারণ `firestore.rules` সত্যিকারের "হাই সিকিউরিটি"
করতে হলে শুধু rules ফাইল বদলালে হয় না — পাসওয়ার্ড Firestore-এ সাধারণ টেক্সটে রাখাটাই মূল সমস্যা,
সেটা rules দিয়ে ঠিক করা যায় না। তাই:

- পাসওয়ার্ড এখন **Firebase Authentication**-এ (Firestore-এ কোথাও পাসওয়ার্ড থাকে না)
- `firestore.rules` এখন **deny-by-default** — যা খোলা রাখা দরকার শুধু তা-ই স্পষ্ট করে খোলা
- সদস্যের ব্যক্তিগত তথ্য (`members`) শুধু অ্যাডমিন ও সংশ্লিষ্ট সদস্য নিজে পড়তে পারেন
- verify.html-এর জন্য আলাদা `public_members` কালেকশন — শুধু প্রয়োজনীয় পাবলিক তথ্য, তা-ও শুধু অনুমোদিত সদস্যের

> ⚠️ **এটা একটা মাইগ্রেশন, শুধু ফাইল বদলানো নয়।** ভুল ক্রমে করলে সব সদস্য লগইন হারাতে পারেন।
> নিচের **§ ২ রোলআউট** ধাপে ধাপে, ক্রম মেনে করুন। প্রতিটা ধাপ থামিয়ে যাচাই করা যায়।

---

## ১. এক নজরে — কী কী বদলেছে

| আগে (v2) | এখন (v3) |
|---|---|
| পাসওয়ার্ড `members.stored_password`-এ সাধারণ টেক্সটে | পাসওয়ার্ড **Firebase Authentication**-এ (কখনো Firestore-এ যায় না) |
| `members` কালেকশন **সবাই পড়তে পারত** | শুধু অ্যাডমিন ও নিজের ডেটা (ইমেইল-ভেরিফাইড লগইন) |
| যে কেউ যেকোনো সদস্যের পাসওয়ার্ড বদলে দিতে পারত | কেউ পারে না — Firebase Auth নিজেই সামলায় |
| লগইন: সদস্য আইডি/ইমেইল/মোবাইল + পাসওয়ার্ড | লগইন: **ইমেইল** + পাসওয়ার্ড (Firebase Auth-এর নিয়ম) |
| পাসওয়ার্ড রিসেট: EmailJS দিয়ে সাধারণ টেক্সট ইমেইলে পাসওয়ার্ড পাঠানো | Firebase নিজেই "পাসওয়ার্ড সেট করুন" লিংক পাঠায়; সাধারণ টেক্সট পাসওয়ার্ড কোথাও যায় না |
| verify.html সরাসরি `members` কালেকশন থেকে পড়ত (সব পাবলিক) | নতুন `public_members` কালেকশন — শুধু approved সদস্যের সীমিত তথ্য, মোবাইল মাস্ক করা |
| রেজিস্ট্রেশনে ডুপ্লিকেট ইমেইল ক্লায়েন্ট-সাইডে চেক হতো (এড়ানো যেত) | সার্ভার-সাইডে (rules-এ) নিশ্চিত করা — এড়ানো যায় না |

---

## ২. রোলআউট — এই ক্রমেই করুন

### ধাপ ০ — Firebase Console-এ প্রস্তুতি (৫ মিনিট)
1. **Authentication → Sign-in method → Email/Password চালু করুন** (এটা ছাড়া কেউ লগইনই করতে পারবেন না)।
2. **Authentication → Settings → Authorized domains** — আপনার সাইটের ডোমেইন (যেমন `your-site.vercel.app`
   বা কাস্টম ডোমেইন) যোগ করুন। না করলেও লগইন কাজ করবে, শুধু পাসওয়ার্ড-রিসেট ইমেইলের
   "সাইটে ফিরে যান" লিংকটা থাকবে না।
3. (ঐচ্ছিক) **Authentication → Templates** — পাসওয়ার্ড রিসেট ও ইমেইল ভেরিফিকেশন টেমপ্লেট বাংলায় ও
   আপনার ব্র্যান্ডিং অনুযায়ী কাস্টমাইজ করতে পারেন।
4. **অ্যাডমিন ইমেইল তালিকা মিলিয়ে নিন** — `firestore.rules` ও `firestore.rules.final` দুটোতেই
   `adminEmails()` ফাংশনে আছে `imran.info.me@gmail.com`। আপনার আসল অ্যাডমিন ইমেইল(গুলো) এখানে
   বসান (একাধিক হলে কমা দিয়ে অ্যারেতে)। `.tz/.js/admin/js/admin-config.js`-এর `ADMIN_EMAILS`-এর
   সাথে **হুবহু মিলতে হবে**।

### ধাপ ১ — ট্রানজিশন rules ও নতুন ফাইল ডিপ্লয় করুন
1. `firestore.rules` (এই জিপে যেটা আছে — **ট্রানজিশন সংস্করণ**) Firebase Console-এ পাবলিশ করুন।
   এটা পুরনো rules-এর হুবহু আচরণ রাখে (তাই পুরনো লগইন এখনো চলবে) — শুধু নতুন দুটো কালেকশন
   (`member_emails`, `public_members`) ব্যবহারের অনুমতি যোগ করে।
2. বাকি সব ফাইল (নিচে § ৩ দেখুন) প্রজেক্টে বসিয়ে deploy করুন।
3. এই মুহূর্তে: **নতুন v3 লগইন পেজ কাজ করবে না** (কারণ কোনো সদস্যের Firebase Auth অ্যাকাউন্ট এখনো নেই) —
   এটাই প্রত্যাশিত। পরের ধাপে সেটা ঠিক হবে।

### ধাপ ২ — অ্যাডমিন প্যানেল থেকে মাইগ্রেশন চালান
অ্যাডমিন প্যানেলে লগইন করে বাঁ পাশের মেনুতে **"নিরাপত্তা আপগ্রেড"** পেজে যান। চারটা ধাপ ক্রমে চাপুন:

1. **স্ক্যান করুন** — কিছু বদলায় না, শুধু রিপোর্ট দেয়: কতজন সদস্য, ইমেইল ঠিক/ভুল/ডুপ্লিকেট কিনা।
   **ডুপ্লিকেট ইমেইল থাকলে** রিপোর্টে দেখাবে কাকে রাখা হবে — বাকিদের ইমেইল হাতে ঠিক করে নিন
   (নইলে তারা লগইন করতে পারবেন না, কারণ ইমেইল ইউনিক হতে হয়)।
2. **ইনডেক্স সিঙ্ক করুন** — প্রতিটি সদস্যের জন্য `member_emails` ও (অনুমোদিতদের) `public_members`
   ডকুমেন্ট বানায়। ইমেইল ছোট হাতের করে দেয়। এটা নিরাপদে বারবার চালানো যায়।
3. **অ্যাকাউন্ট তৈরি করুন** — প্রতিটি *অনুমোদিত* সদস্যের জন্য Firebase Authentication অ্যাকাউন্ট
   বানায় (একটা এলোমেলো গোপন পাসওয়ার্ডে, যা কখনো ব্যবহার হয় না)। **কাউকে কোনো ইমেইল যায় না এই ধাপে।**
4. **পুরনো পাসওয়ার্ড মুছুন** — *শুধু নিচের যাচাই শেষে করুন।* এটা `members` থেকে সব `stored_password`
   ফিল্ড চিরতরে মুছে দেয়।

### ধাপ ৩ — নিজে একবার লগইন করে যাচাই করুন
1. নিজের (বা একজন টেস্ট সদস্যের) ইমেইল দিয়ে সদস্য লগইন পেজে "পাসওয়ার্ড পাননি বা ভুলে গেছেন?" চাপুন।
2. ইমেইলে আসা লিংক থেকে পাসওয়ার্ড সেট করুন।
3. সেই ইমেইল ও নতুন পাসওয়ার্ড দিয়ে লগইন করে ড্যাশবোর্ড, QR, নোটিশ, ইভেন্ট — সব ঠিকমতো দেখাচ্ছে
   কিনা দেখুন।
4. verify.html-এ সেই সদস্যের QR/লিংক দিয়ে ভেরিফাই পেজ ঠিকমতো খুলছে কিনা দেখুন।

### ধাপ ৪ — চূড়ান্ত কড়া rules ডিপ্লয় করুন
সব ঠিকমতো চলছে নিশ্চিত হলে, **ধাপ ২.৪ (পুরনো পাসওয়ার্ড মুছুন)** চালিয়ে দিন, তারপর
`firestore.rules.final`-এর কন্টেন্ট Firebase Console-এ পাবলিশ করুন (ফাইলটার নাম বদলে
`firestore.rules` রাখুন, বা কনসোলে কপি-পেস্ট করুন)। এখন থেকে:
- `members` আর সবার জন্য পড়া যাবে না
- পাসওয়ার্ড সংক্রান্ত কোনো ফিল্ড লেখার অনুমতিই rules-এ নেই

**যদি কোনো ধাপে আটকে যান:** ধাপ ১-এর ট্রানজিশন rules-এই থেকে যান (এটা অনির্দিষ্টকাল নিরাপদে চলতে
পারে, শুধু পুরনো দুর্বলতা — সবার members পড়া/লেখা — তখনও থাকে)। নতুন সদস্যরা নতুন ব্যবস্থায়,
পুরনোরা পুরনো ব্যবস্থায় — দুটোই সমান্তরালে কাজ করে, তাড়াহুড়োর দরকার নেই।

---

## ৩. ফাইল তালিকা

| ফাইল | কাজ |
|---|---|
| `firestore.rules` | **ধাপ ১-এ পাবলিশ করুন** — ট্রানজিশন (পুরনো + নতুন কালেকশন) |
| `firestore.rules.final` | **ধাপ ৪-এ পাবলিশ করুন** — চূড়ান্ত কড়া rules |
| `js/login-loader.js` | লেজি-লোডার (অপরিবর্তিত কাঠামো, শুধু storage key নাম মিলিয়ে নেওয়া) |
| `js/login-core.js` | আইকন, তারিখ/সময়, টোস্ট, ক্লিপবোর্ড (পাসওয়ার্ড-জেনারেটর অংশ সরানো হয়েছে — আর দরকার নেই) |
| `js/login-auth.js` | **সম্পূর্ণ নতুন** — Firebase Authentication দিয়ে সাইন-ইন, ইমেইল ভেরিফিকেশন, রিসেট, সেশন |
| `js/login-qr.js` | অপরিবর্তিত (পিওর JS QR জেনারেটর) |
| `js/login.js` | ইমেইল+পাসওয়ার্ড ফর্ম, ভেরিফিকেশন UI, নতুন রিসেট ডায়ালগ, সেশন/রিভ্যালিডেশন |
| `js/dashboard-notices.js`, `js/dashboard-events.js` | notices/events-এ `permission-denied` হলে ("শুধু অনুমোদিত সদস্যরা") আলাদা বার্তা |
| `css/login.css` | আপডেট (ইমেইল ভেরিফিকেশন বক্সের স্টাইল যোগ) |
| `js/registration.js` | নতুন আবেদন এখন **এক ব্যাচে** (`members` + `member_emails`) জমা হয় — ডুপ্লিকেট ইমেইল সার্ভার-সাইডে আটকায় |
| `js/verify/member-id.js` | আইডি sanitize (শুধু A-Z0-9-, ইনজেকশন ঠেকাতে) |
| `js/verify/member-lookup.js` | এখন `public_members`-এ ID দিয়ে সরাসরি পড়ে (আগে `members`-এ query করত) |
| `js/verify/render-member-info.js` | সব মান HTML-escape করে দেখানো হয় |
| `.tz/.js/admin/js/firebase.js` | নতুন এক্সপোর্ট: `writeBatch`, `getDoc`, `setDoc`, `deleteField`, ও সদস্য-অ্যাকাউন্ট তৈরির জন্য Auth ফাংশন |
| `.tz/.js/admin/js/provision.js` | **নতুন** — সদস্যের Firebase লগইন অ্যাকাউন্ট তৈরি ও পাসওয়ার্ড-সেটআপ লিংক পাঠানো |
| `.tz/.js/admin/js/sync.js` | **নতুন** — প্রতিটি সদস্য-পরিবর্তনে `member_emails`/`public_members` সিঙ্কে রাখার লজিক |
| `.tz/.js/admin/js/security.js` | **নতুন** — "নিরাপত্তা আপগ্রেড" মাইগ্রেশন পেজ (স্ক্যান/সিঙ্ক/অ্যাকাউন্ট/ক্লিন) |
| `.tz/.js/admin/js/members.js` | সংরক্ষণ/ডিলিট এখন atomic batch; অনুমোদন করলে লগইন অ্যাকাউন্ট তৈরির বাটন |
| `.tz/.js/admin/js/app.js` | নতুন রুট ("security"), অ্যাডমিন-ইমেইল মিল case-insensitive |
| `.tz/.js/admin/index.html` | সাইডবারে "নিরাপত্তা আপগ্রেড" লিংক |
| `.tz/.js/admin/css/admin.css` | নতুন পেজের জন্য স্টাইল যোগ (আগের স্টাইল অক্ষত) |
| `sw.js` | `CACHE_VERSION` বাড়ানো — deploy করলে ফিরতি ভিজিটররা নতুন কোড পাবেন |

`index.html`-এ নতুন কোনো পরিবর্তন নেই — আগে যোগ করা লেজি-লোডার কাঠামোই এখনো ব্যবহার হচ্ছে।

---

## ৪. কনফিগ (`js/login.js`-এর মাথায়)

| অপশন | ডিফল্ট | কাজ |
|---|---|---|
| `firebaseConfig` / `firebaseSdkVersion` | (আপনার প্রজেক্টের) | Firebase SDK কনফিগ |
| `authContinueUrl` | `""` (ফাঁকা → সাইটের `/#/login`) | পাসওয়ার্ড-সেট/ভেরিফিকেশন লিংকে ক্লিকের পর কোথায় ফিরবেন |
| `deniedStatuses` | `["blocked","rejected"]` | এই status-এ লগইন বন্ধ |
| `pendingStatuses` | `["pending"]` | এই status-এ "অপেক্ষায়" নোটিস |
| `rememberDays` / `sessionHours` | `30` / `8` | "লগইন থাকুন" মেয়াদ / সাধারণ সেশন মেয়াদ |
| `idleMinutes` / `idleWarnSeconds` | `20` / `60` | নিষ্ক্রিয়তার সীমা (`0`=বন্ধ) |
| `maxAttempts` / `lockSeconds` | `5` / ধাপে ধাপে বাড়ে | **এই ডিভাইসে** অতিরিক্ত UX-সীমা (আসল রেট-লিমিট Firebase সার্ভারে) |
| `resetCooldownSeconds` / `resetMaxPerHour` | `60` / `5` | পাসওয়ার্ড-রিসেট/ভেরিফিকেশন ইমেইলের সীমা (এই ডিভাইসে) |
| `eventTzOffsetMin` | `360` | ইভেন্টের `.ics`-এ টাইমজোন (বাংলাদেশ = UTC+6) |

`firestore.rules` ও `firestore.rules.final`-এর মাথায়: `adminEmails()` (বাধ্যতামূলক মিলিয়ে নিন),
`adminUids()` (ঐচ্ছিক অতিরিক্ত সুরক্ষা — Firebase Console থেকে UID বসালে ইমেইল+UID দুটোই মিলতে হবে)।

---

## ৫. এখন কী কী সুরক্ষিত, কী এখনো করণীয়

**এখন যা ঠিক হয়েছে:**
- পাসওয়ার্ড সাধারণ টেক্সটে কোথাও নেই; ব্রাউজারে যাচাই হয় না
- `members`/`member_emails` আর সবার জন্য খোলা নয়
- verify.html পাবলিক ডেটা সীমিত ও ইচ্ছাকৃতভাবে প্রকাশযোগ্য অংশে সীমাবদ্ধ (মোবাইল মাস্ক করা)
- ডুপ্লিকেট-ইমেইল আবেদন সার্ভার-সাইডে আটকানো
- ভুল-চেষ্টার রেট-লিমিট এখন Firebase-এর সার্ভারে (আগে শুধু ব্রাউজারে ছিল, এড়ানো সহজ ছিল)

**এখনো যা মাথায় রাখবেন:**
- **`.tz/.js/admin/js/login.js`** (অ্যাডমিন লগইন) এই কাজে বদলানো হয়নি — এটা আগে থেকেই আলাদা
  Firebase Auth ব্যবহার করত, তাই স্বতন্ত্রভাবে নিরাপদ ছিল। দুই সিস্টেম (সদস্য লগইন, অ্যাডমিন লগইন)
  এখন একই Firebase Authentication প্রজেক্ট শেয়ার করে, কিন্তু rules-এ `isAdmin()` ইমেইল-তালিকা দিয়ে
  আলাদা রাখা আছে।
- `notices`/`events`/`team_members`/`donors` কালেকশনের নিয়ম আগের যুক্তিতেই আছে (notices/events
  শুধু অনুমোদিত সদস্য, বাকি দুটো সবার জন্য পাবলিক) — এই প্রজেক্টের চাহিদা অনুযায়ী মনে হয়েছে ঠিক আছে,
  কিন্তু আপনি চাইলে বদলাতে পারেন।
- Firebase Authentication-এর নিজস্ব রেট-লিমিট/স্প্যাম-সুরক্ষা Google-এর হাতে; সেটার সেটিংস
  (যেমন reCAPTCHA Enterprise integration) Firebase Console-এ ঐচ্ছিকভাবে আরও কড়া করা যায়।

---

## ৬. পরীক্ষা

স্বয়ংক্রিয়ভাবে **৪৫২টি চেক** চালিয়ে যাচাই করা হয়েছে, সবগুলো পাস:

- **২০৪টি ব্রাউজার-টেস্ট** (হেডলেস Chromium + নকল Firebase Auth/Firestore) — ফর্ম যাচাই, ইমেইল+পাসওয়ার্ড
  লগইন, ভুল পাসওয়ার্ড/অজানা ইমেইল/ব্লকড/পেন্ডিং/আনভেরিফাইড/নিষ্ক্রিয়-অ্যাকাউন্ট প্রতিটি পথ, Firebase-এর
  রেট-লিমিট বার্তা, লকআউট (রিলোডে টিকে থাকাসহ), ইমেইল-ভেরিফিকেশন পাঠানো ও যাচাই করা, পাসওয়ার্ড-রিসেট
  (পরিচিত/অপরিচিত ইমেইলে একই গোপনীয়তা-নিরাপদ বার্তা), সেশন (রিলোড, নতুন ট্যাব, ক্রস-ট্যাব লগআউট,
  মেয়াদ, সদস্যপদ ব্লক/Firebase অ্যাকাউন্ট মুছে গেলে সাইন-আউট), ড্যাশবোর্ড, নোটিশ/ইভেন্টে
  permission-denied বার্তা (পেন্ডিং/ব্লকড সদস্যের জন্য), QR ও ডাউনলোড করা কার্ড OpenCV দিয়ে ডিকোড করে
  যাচাই, XSS ইনজেকশন-চেষ্টা, নিষ্ক্রিয়তা/অফলাইন/নেভিগেশন, অ্যাক্সেসিবিলিটি, ৩২০px–১৪৪০px লেআউট,
  লেজি-লোডার।
- **১১৯টি Firestore rules টেস্ট** (নিজস্ব rules-ইন্টারপ্রেটার দিয়ে) চূড়ান্ত কড়া rules-এর উপর —
  প্রতিটা কালেকশনে কে পড়তে/লিখতে/লিস্ট করতে পারে, আবেদনের প্রতিটা যাচাই-শর্ত, ভুয়া ইনডেক্স/পাবলিক-ডক
  তৈরির চেষ্টা। **মিউটেশন টেস্টিং**ও করা হয়েছে (rules-এ ইচ্ছাকৃত ২৫টা ফুটো তৈরি করে দেখা হয়েছে টেস্ট
  সেগুলো ধরে কিনা — ২৩টা ধরেছে, বাকি ২টা অন্য শর্তে already covered হওয়ায় নিরাপদে "survive" করেছে)।
- **১৯টি টেস্ট** ট্রানজিশন rules-এর উপর — পুরনো লগইন/ভেরিফাই এখনো ঠিকমতো চলে তা নিশ্চিত করতে।
- **৩৯টি ইউনিট টেস্ট** অ্যাডমিনের sync.js-এর উপর (মোবাইল মাস্কিং, পাবলিক-ডেটা শেপিং, protocol-relative
  URL প্রত্যাখ্যান ইত্যাদি)।
- **২৬টি টেস্ট** পুরো "নিরাপত্তা আপগ্রেড" মাইগ্রেশন টুলের উপর — এলোমেলো বাস্তবসম্মত ডেটা (ডুপ্লিকেট
  ইমেইল, বড় হাতের ইমেইল, খারাপ মেম্বার আইডি, খালি ইমেইল) দিয়ে স্ক্যান/সিঙ্ক/অ্যাকাউন্ট-তৈরি/পাসওয়ার্ড-মোছা
  চালিয়ে প্রতিটা ধাপ সঠিক আচরণ করছে কিনা।
- প্রজেক্টের **সবগুলো (৬৬টি) `.js` ফাইল** সিনট্যাক্স-চেক করা হয়েছে।

**যা এখানে পরীক্ষা করা যায়নি (আপনি § ২ ধাপ ৩ অনুযায়ী একবার নিজে যাচাই করবেন):**
- আসল Firebase Authentication ও আসল Firestore-এর সাথে (নকল দিয়ে চালানো হয়েছে — API কলগুলো সঠিক
  ও ডকুমেন্টেশন-অনুসারী, কিন্তু আসল প্রজেক্টে একবার হাতে-কলমে চালিয়ে দেখাই সবচেয়ে নিশ্চিত)।
- Firebase-এর reCAPTCHA বা অন্য কোনো অতিরিক্ত সুরক্ষা ফিচার (এগুলো Console থেকে সেট করা, কোডে নয়)।

---

## ৭. পরের সম্ভাব্য ধাপ
- verify.html-এ রেট-লিমিটিং (Cloud Function দিয়ে) যোগ করা যায়, যাতে কেউ একটার পর একটা ID
  অনুমান করে সদস্যদের তথ্য ঘাঁটতে না পারে (member_id র‍্যান্ডম-সংখ্যা-ভিত্তিক বলে অনুমান এমনিতেই কঠিন,
  তবু অতিরিক্ত স্তর হিসেবে যোগ করা যায়)।
- অ্যাডমিন ও সদস্য লগইন এখন একই Firebase প্রজেক্ট শেয়ার করে (rules-এ ইমেইল-তালিকা দিয়ে আলাদা);
  চাইলে ভবিষ্যতে সম্পূর্ণ আলাদা Firebase প্রজেক্টে সরানো যায় (অতিরিক্ত সুরক্ষা স্তর, ঐচ্ছিক)।
- ডার্ক মোড (রঙ টোকেন আলাদা করা আছে, যোগ করা সহজ)।
